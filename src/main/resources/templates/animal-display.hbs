<!DOCTYPE html>
<html>
<head>
    <title>WildLife Tracker</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark static-top">
    <div class="container">
        <a class="navbar-brand" href="#">
            <img src="https://i.postimg.cc/ZnkWVqDP/lion.jpg" alt="..." height="36">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/normal-sightings/new">Record Normal Animal Sighting</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/endangered-sightings/new">Record Endangered Animal Sighting</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
<div class="container">
    <!--begin main template-->

    <div class="col-md-12">
        <h1 class="text-center display-1">View The Animal</h1>

        {{#if normal}}
            <p>Animal ID: {{normalAnimal.id}}</p>
            <p>Name of Animal: {{normalAnimal.name}}</p>
            <p>Type of Animal: {{normalAnimal.type}}</p>
        {{/if}}

        {{#if endangered}}
            <div class="card-header">Id of animal: {{endangeredAnimal.id}}</div>
            <p>Name of animal: {{endangeredAnimal.name}}</p>
            <p>Health of animal: {{endangeredAnimal.health}}</p>
            <p>Age of animal: {{endangeredAnimal.age}}</p>
            <p>Type of Animal: {{endangeredAnimal.type}}</p>
        {{/if}}
    </div>
</div>
<!--end main template-->
</body>
</html>